-- first version
(def counter (n) 
  (do 
    ((def inc () 
      (do 
        ((set n (+ n 1)))
        n)))
    inc))

-- alternative using lambda function
(def ctr (n) 
  (lambda () (do ((set n (+ n 1))) n)))

(set c (ctr 0))
(c)
(c)
